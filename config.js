// ---------------------------------------------------------
// Fill in these two values from your Cloudinary account,
// then this whole site works. Nothing else needs to change.
// ---------------------------------------------------------
const CLOUDINARY_CLOUD_NAME = "YOUR_CLOUD_NAME";   // e.g. "dabc123xy"
const CLOUDINARY_UPLOAD_PRESET = "YOUR_UPLOAD_PRESET"; // e.g. "wedding_unsigned"

// A tag applied to every guest upload, so they're easy to find and
// filter inside your own Cloudinary Media Library. No need to change this.
const CLOUDINARY_TAG = "brenda-wayne-wedding";

// The code guests type in before they can see or upload anything.
// Print this beneath your QR code. Not case sensitive.
const ACCESS_CODE = "REPLACE_ME"; // e.g. "BW2026"
